{
  "name": "the-band-alibi",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "website:dev": "cd website && yarn dev && cd ..",
    "studio:dev": "cd studio && yarn start && cd ..",
    "dev": "concurrently \"yarn website:dev\" \"yarn studio:dev\""
  },
  "dependencies": {},
  "devDependencies": {
    "@types/react-swipeable-views": "^0.13.1",
    "concurrently": "^6.0.0"
  }
}
